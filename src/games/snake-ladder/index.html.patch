--- /Users/kalikali/Desktop/1click/src/games/snake-ladder/index.html
+++ /Users/kalikali/Desktop/1click/src/games/snake-ladder/index.html
@@ -354,12 +354,12 @@
     .board-wrap img.board-img {
       width:100%; height:100%;
       object-fit:contain;
       display:block;
       border-radius:8px;
     }
     #tokensSvg {
       position:absolute; inset:0;
       pointer-events:none;
       z-index:20;
-      border:2px solid red;
     }
 
     /* ── PLAYER PANELS ── */
@@ -641,7 +641,7 @@
 
     <div class="board-wrap">
       <img class="board-img" src="assets/board.webp" alt="Snake and Ladder Board" loading="eager">
-      <svg id="tokensSvg" viewBox="0 0 100 100" preserveAspectRatio="none"></svg>
+      <svg id="tokensSvg" viewBox="0 0 100 100" preserveAspectRatio="xMidYMid meet"></svg>
     </div>
 
     <div class="controls-row">
@@ -754,23 +754,42 @@
 //  COORDINATE HELPER
 // ═══════════════════════════════════════════════════
 function cellCenter(num) {
-  // Board: 10x10 grid with zig-zag pattern
-  // Row 0 (from bottom): 1-10 (left to right)
-  // Row 1 (from bottom): 11-20 (right to left)
-  // Row 2 (from bottom): 21-30 (left to right)
-  // etc.
+  // Based on precise pixel calibration of the 640x640 board image
+  // The board is NOT perfectly uniform and has outer borders
   
-  // Which row from the bottom (0-9)
+  // Exact column centers calibrated from the image (0-100% SVG coordinates)
+  const colCenters = [
+    4.84,  // Col 0
+    14.84, // Col 1
+    24.84, // Col 2
+    34.84, // Col 3
+    44.84, // Col 4
+    55.00, // Col 5
+    65.00, // Col 6
+    75.00, // Col 7
+    85.00, // Col 8
+    95.00  // Col 9
+  ];
+  
+  // Exact row centers calibrated from the image (0-100% SVG coordinates, 0=top)
+  // Row 0 is the bottom row (cells 1-10)
+  const rowCenters = [
+    95.31, // Row 0 (bottom)
+    85.94, // Row 1
+    76.25, // Row 2
+    66.41, // Row 3
+    56.41, // Row 4
+    46.25, // Row 5
+    36.09, // Row 6
+    25.63, // Row 7
+    15.31, // Row 8
+    5.00   // Row 9 (top)
+  ];
+
   const row = Math.floor((num - 1) / 10);
   const colInRow = (num - 1) % 10;
   
-  // Calculate column based on zig-zag
-  let col;
-  if (row % 2 === 0) {
-    // Even rows (0, 2, 4, 6, 8): left to right
-    col = colInRow;
-  } else {
-    // Odd rows (1, 3, 5, 7, 9): right to left
-    col = 9 - colInRow;
-  }
+  // Zig-zag pattern: even rows go left-to-right, odd rows go right-to-left
+  const col = (row % 2 === 0) ? colInRow : 9 - colInRow;
   
-  // Center of the cell (each cell is 10x10 in SVG coordinates)
-  // Row 0 (bottom) has y=95, Row 9 (top) has y=5
-  const x = col * 10 + 5;
-  const y = (9 - row) * 10 + 5;
+  const x = colCenters[col];
+  const y = rowCenters[row];
   
   return { x, y };
 }
